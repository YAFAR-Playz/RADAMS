import type { TourStep } from "./types";

// Built last — a superset walkthrough, reusing data-tour selectors already
// added for the other roles' equivalent screens where possible.
export const ADMIN_TOUR: TourStep[] = [];
